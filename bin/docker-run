#!/usr/bin/env bash

nix run .\#x86_64-linux.matomo.oci-images.matomo.copyToDockerDaemon
image=$(nix eval --raw .\#x86_64-linux.matomo.oci-images.matomo --apply '(i: "${i.imageName}:${i.imageTag}")')
exec docker run --rm -it $image "$@"
